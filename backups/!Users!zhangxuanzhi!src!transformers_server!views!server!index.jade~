extend ../layout

block content
  h1 Server List
  table.table.table-hover.table-condensed
    thead
      tr
        th id
        th name
        th url
        th status
        th allowNew
        th modify
    tbody
      each server in servers
        tr
          th #{ server._id}        
          th #{ server.name }
          th #{ server.url }
          th #{ statusTable[server.status] }
          th #{ boolTable[server.allowNew] }
          th
            form(action='/admin/server/update', method='post')
              input(type='hidden', name='id', value=server._id)
              input.btn.btn-warning(type='submit', value='修改')
  a.btn.btn-default(type='button', href='/admin/server/new') Create